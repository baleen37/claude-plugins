{
  "$schema": "../../../schemas/hooks-schema.json",
  "description": "Auto-updater hooks - checks for plugin updates on session start",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "npx tsx ${CLAUDE_PLUGIN_ROOT}/src/hooks/auto-update-hook.ts"
          }
        ]
      }
    ]
  }
}
