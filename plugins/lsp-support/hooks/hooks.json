{
  "description": "Auto-install LSP servers if not found",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bun ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/check-install-lsps.ts",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
