name: CI

on:
  pull_request:
    branches: ['**']
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Install jq
        uses: sergeysova/jq-action@v2

      - name: Install yq
        uses: mikefarah/yq@v4.40.1

      - name: Setup Bats and bats libs
        run: |
          # Install Bats without caching to avoid permission issues
          git clone --depth 1 --branch v1.11.0 https://github.com/bats-core/bats.git /tmp/bats
          cd /tmp/bats && ./install.sh $HOME/.local
          echo "$HOME/.local/bin" >> $GITHUB_PATH

          # Install bats-support
          git clone --depth 1 --branch v0.3.0 https://github.com/bats-core/bats-support.git /tmp/bats-support
          cd /tmp/bats-support && sudo mkdir -p /usr/lib/bats-support
          sudo ./install.sh /usr/lib/bats-support

          # Install bats-assert
          git clone --depth 1 --branch v2.1.0 https://github.com/bats-core/bats-assert.git /tmp/bats-assert
          cd /tmp/bats-assert && sudo mkdir -p /usr/lib/bats-assert
          sudo ./install.sh /usr/lib/bats-assert

          # Install bats-file
          git clone --depth 1 --branch v0.4.0 https://github.com/bats-core/bats-file.git /tmp/bats-file
          cd /tmp/bats-file && sudo mkdir -p /usr/lib/bats-file
          sudo ./install.sh /usr/lib/bats-file

      - name: Run all tests
        shell: bash
        env:
          BATS_LIB_PATH: /usr/lib
          TERM: xterm
        run: bash tests/run-all-tests.sh
